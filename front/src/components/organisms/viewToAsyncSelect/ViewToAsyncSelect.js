import React from 'react';
import PropTypes from 'prop-types';

const ViewToAsyncSelect = props => {
  return (
    <div>

    </div>
  );
};

ViewToAsyncSelect.propTypes = {

};

export default ViewToAsyncSelect;
